server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: //Repo de github
          search-paths: config-data
  application:
    name: config-service

