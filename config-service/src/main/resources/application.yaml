server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/ghigoviu/spring-ms-tuto
          search-paths: config-data
  application:
    name: config-service

